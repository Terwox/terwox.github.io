<!-- JAMES-CONFIG:OMX:START -->
## OMX / James Config Guidance

- Universal Codex guidance lives in `D:\git\James-LLM-Agent-Config\AGENTS.md`; before doing project work, read that file directly and keep it in context. It is the canonical global Codex guidance and it points to additional skills/overlays when relevant.
- OMX is installed user-wide. Load workflow skills from `C:\Users\terwo\.codex\skills`, including `$deep-interview`, `$ralplan`, and `$ralph` when the task calls for them.
- Keep this repo's OMX runtime state in `D:\git\terwox.github.io\.omx`. Do not write project state into `D:\git\James-LLM-Agent-Config`.
- Treat `.omx/` as local/untracked workflow state unless a human explicitly asks to preserve or commit a specific artifact.
<!-- JAMES-CONFIG:OMX:END -->

# terwox.github.io Agent Instructions

## Local Notes
- Add repo-specific architecture notes, workflow conventions, and verification commands here.
